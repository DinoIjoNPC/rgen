{
  "name": "forge-grok-image-gen",
  "version": "1.0.0",
  "description": "Backend proxy untuk generate gambar via Grok API, deploy di Railway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
